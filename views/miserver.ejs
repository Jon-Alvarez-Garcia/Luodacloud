<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Mis Servidores Contratados</title>
  <link rel="stylesheet" href="/styles/style.css">
</head>
<body>

<br/>
<br/>

  <h1>Mis Servidores Contratados</h1>

  <% if (servidores.length === 0) { %>
    <p class="no-servers">No tienes servidores contratados aún.</p>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Memoria RAM</th>
          <th>Slots</th>
          <th>Estado</th>
          <th>Fecha de Creación</th>
          <th>Tu dirección</th>
        </tr>
      </thead>
      <tbody>
        <% servidores.forEach(serv => { %>
          <tr>
            <td><%= serv.id %></td>
            <td><%= serv.memoria_ram %> MB</td>
            <td><%= serv.slots %> jugadores</td>
            <td><%= serv.estado %></td>
            <td><%= new Date(serv.fecha_creacion).toLocaleString() %></td>
            <td>luodacloud.com:<%= serv.puerto %></td>
            <td>
              <a href="/detalles/<%= serv.id %>">Ver detalles</a>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } %>

  <div class="back-link">
    <a href="/contratar.html">Volver a contratar servidor</a>
  </div>

  <video autoplay muted loop id="backgroundVideo">
    <source src="/videos/videoplayback.mp4" type="video/mp4">
    Tu navegador no soporta el elemento de video.
  </video>
  

</body>
</html>
